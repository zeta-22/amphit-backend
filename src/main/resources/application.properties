# Server Configuration
server.port=8080
server.servlet.context-path=/api

# PostgreSQL Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/amphit
spring.datasource.username=postgres
spring.datasource.password=Pianetax1!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
app.jwt.secret=amphitSuperSecretKeyThatShouldBeStoreMoreSecurelyInProductionAtLeast32BytesLong
app.jwt.expiration=86400000
app.jwt.header=Authorization
app.jwt.prefix=Bearer 

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.amphit=DEBUG
